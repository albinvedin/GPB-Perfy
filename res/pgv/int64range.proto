syntax = "proto3";

package pgv;

import "validate/validate.proto";

message Int64Range {
  repeated int64 content = 1 [
      (validate.rules).repeated.items.int64 = {gt: -100, lt: 100}
    ];
}

message FloatRange {
  repeated float content = 1 [(validate.rules).repeated.items.float = {gt: -100, lt: 100}];
}

message DoubleRange {
  repeated double content = 1 [(validate.rules).repeated.items.double = {gt: -100, lt: 100}];
}
